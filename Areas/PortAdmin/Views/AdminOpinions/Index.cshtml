@model IEnumerable<MyPortfolio.Models.Opinion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/PortAdmin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Opinionİmg)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OpinionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OpinionDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td width="25%">
            <img style="width:100%" class="img-fluid" src="@item.Opinionİmg" alt="Alternate Text" />
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OpinionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OpinionDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
