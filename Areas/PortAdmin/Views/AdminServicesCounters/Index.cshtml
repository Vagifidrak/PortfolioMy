@model IEnumerable<MyPortfolio.Models.ServicesCounter>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/PortAdmin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Counterİcon)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CounterNum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CounterText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Counterİcon)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CounterNum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CounterText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
